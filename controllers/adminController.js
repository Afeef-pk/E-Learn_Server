const adminCollection = require('../models/adminModel')

const handleAdminLogin = async(req,res)=>{
    try {
        
    } catch (error) {
        next(error)
    }
}


module.exports = {
    handleAdminLogin
}